# 贡献指南

## 1. 分支策略
- `main`（或默认保护分支）：稳定可发布
- `feat/*`：新功能
- `fix/*`：缺陷修复
- `chore/*`：工程维护

## 2. Commit 规范（建议）
使用 Conventional Commits：
- `feat:` 新功能
- `fix:` 修复问题
- `docs:` 文档变更
- `chore:` 构建/流程/依赖等

示例：
```text
docs: add onboarding and contribution guide
```

## 3. Pull Request 要求
每个 PR 至少包含：
1. **背景**：为什么要改
2. **改动内容**：改了什么
3. **验证方式**：如何验证改动有效
4. **影响范围**：是否影响已有行为

## 4. Code Review 关注点
- 可读性：命名是否表达意图
- 可维护性：是否存在重复逻辑
- 可测试性：是否可验证
- 风险：边界条件和回滚方案

## 5. 文档要求
涉及下列情况必须更新文档：
- 新增模块或关键流程
- 修改对外接口
- 调整部署/运行方式
